# Pure-integer compute loop — the register-allocating JIT's target workload.
fn compute(n):
    set acc = 0
    set i = 0
    while i < n:
        acc = acc + (i * 3 + 7) % 13
        acc = acc % 30011
        i = i + 1
    return acc
say compute(30000000)
